# Smart Medical PowerPoint Translator 🚀

شخصی‌سازی هوشمند PowerPoint برای متخصصان پزشکی - سیستم پیشرفته ترجمه تخصصی با هوش مصنوعی

## ✨ ویژگی‌های کلیدی

- 🤖 **AI پیشرفته** Gemini 2.5 Flash & Pro
- 🧠 **تشخیص هوشمند متن** - فقط متن‌های قابل ترجمه
- 🔍 **تحقیق خودکار منبع** - شناسایی حوزه تخصصی
- 🎨 **فرمت فارسی هوشمند** - فونت B Nazanin + راست‌چین
- 📊 **گزارش واژه‌ها** - PDF تخصصی اصطلاحات پزشکی
- 🔒 **پیش‌بینی امنیتی** - تست ورودی‌ها

## 🛠️ تکنولوژی‌ها

**Backend:**
- Python 3.11+ FastAPI
- Gemini 2.5 AI Models
- Python PPTX, ReportLab
- PostgreSQL (اختیاری)

**Frontend:**
- Next.js 14 + React 18
- TypeScript + Tailwind CSS
- Axios برای API calls

---

## 📦 نصب و راه‌اندازی سریع

### گام ۱: پیش‌نیازها
```bash
# Python 3.11+
# Node.js 18+
# Git

# چک کردن نسخه‌ها
python --version
node --version
npm --version
```

### گام ۲: کلون و نصب
```bash
# کلون پروزه
git clone [repository-url]
cd smart-medical-translator

# یا مستقیم اجرا کنید
cd 1/project_1

# اجرای خودکار کامل (پیشنهاد می‌شود)
./run_project.bat

# یا مرحله به مرحله
```

### گام ۳: تنظیم API Key
```bash
# فایل backend/.env را ویرایش کنید
echo "GEMINI_API_KEY=your_actual_gemini_api_key_here" > backend/.env
```

---

## 🚀 اجرای پروژه

### راه سریع (One-Click):
```bash
# فقط فایل batch را اجرا کنید
run_project.bat
```
این فرمان خودکار تمامی dependencyها را نصب و سرورها را اجرا می‌کند.

### راه دستی:

**Backend:**
```bash
cd backend
pip install -r requirements.txt
python main.py
```

**Frontend (در ترمینال جدید):**
```bash
cd frontend
npm install
npm run dev
```

### چک وضعیت:
- **Frontend:** http://localhost:3000
- **Backend API:** http://127.0.0.1:8000

---

## 🎨 نحوه استفاده

### ۱. انتخاب فایل
- فایل PowerPoint (.pptx) را انتخاب کنید
- حداکثر اندازه: ۵۰ مگابایت

### ۲. نام منبع را وارد کنید
```
مثال‌های خوب:
- "Retinal Detachment Treatment"
- "Deep Learning in Medical Imaging"
- "Neurological Disorders Diagnosis"
```

### ۳. شروع ترجمه
سیستم هوشمند:
- 🔍 منبع را جستجو و حوزه را تشخیص می‌دهد
- 📝 فقط متن‌های توضیحی را ترجمه می‌کند
- 🎯 از مترجم تخصصی پزشکی استفاده می‌کند
- 🔤 فرمت فارسی را اعمال می‌کند

### ۴. دانلود نتایج
- **فایل PowerPoint ترجمه‌شده** با فرمت فارسی
- **گزارش PDF واژه‌ها** با اصطلاحات تخصصی

---

## 🤖 مدل‌های AI استفاده شده

| مدل | ویژگی‌ها | کاربرد |
|-----|---------|--------|
| **Gemini 2.5 Flash** (پیش‌فرض) | ⚡ سریع، 💰 ارزان | ترجمه روزانه |
| **Gemini 2.5 Pro** | 🧠 کیفیت بالا، ⏱️ متوسط | ترجمه تخصصی |

### تغییر مدل:
در کد `backend/main.py`:
```python
translator = MedicalTranslationEngine(system_instruction, model_type="pro")
```

---

## 📊 ویژگی‌های هوشمند

### 🎯 تشخیص متن هوشمند:
- ✅ متن‌های توضیحی ترجمه می‌شوند
- ❌ عناوین، کپشن‌ها، جداول، اختصارات ترجمه نمی‌شوند
- 🔤 کلمات تخصصی بدون معادل به انگلیسی می‌مانند

### 🔍 تحقیق هوشمند منبع:
```
ورودی: "Deep Learning in Medical Imaging"
خروجی:
{
  "exact_title": "Deep Learning Applications in Medical Imaging",
  "primary_field": "Medical Imaging",
  "scientific_terms": {"CNN": "شبکه عصبی پیچشی", ...}
}
```

### 🎨 فرمت‌دهی فارسی:
- فونت: B Nazanin (پیش‌فرض)
- تراز: راست‌چین (RTL)
- نگه‌داری: ساختار اصلی اسلاید

---

## 🗂️ ساختار پروژه

```
smart-medical-translator/
├── backend/                    # FastAPI Server
│   ├── main.py                # API اصلی + تمام logic
│   ├── requirements.txt       # Python dependencies
│   └── .env                   # Gemini API key
├── frontend/                  # Next.js App
│   ├── src/app/
│   │   ├── layout.tsx        # تنظیمات فارسی
│   │   ├── page.tsx          # UI اصلی
│   │   └── globals.css       # استایل‌ها
│   └── package.json
├── test/                      # فایل‌های آزمایشی
│   └── Griffin binocular anomalies.pptx
├── run_project.bat           # اجرای خودکار کامل
└── README.md                 # این فایل
```

---

## 🔧 تنظیمات پیشرفته

### تنظیمات AI:
```python
# در backend/main.py
class MedicalTranslationEngine:
    def __init__(self, system_instruction: str, model_type: str = "flash"):
        # تغییر به "pro" برای کیفیت بالاتر
        self.model_type = model_type
```

### تنظیمات Database (اختیاری):
```python
# برای ذخیره تاریخچه
DATABASE_URL=postgresql://user:password@localhost/translator_db
```

### تنظیمات CORS:
```python
# در production تغییر دهید
origins = [
    "https://yourdomain.com",
    "http://localhost:3000"  # فقط برای توسعه
]
```

---

## 🐛 رفع مشکلات

### مشکل: Backend اجرا نمی‌شود
```bash
# dependencyها را دوباره نصب کنید
cd backend
pip install --upgrade -r requirements.txt

# پورت 8000 را چک کنید
netstat -ano | findstr :8000
```

### مشکل: API key نامعتبر
```bash
# API key را دوباره تنظیم کنید
# از Google AI Studio دریافت کنید
echo "GEMINI_API_KEY=YOUR_NEW_KEY" > backend/.env
```

### مشکل: Frontend خطا می‌دهد
```bash
# cache را پاک کنید
cd frontend
rm -rf .next
npm install
npm run dev
```

### مشکل: فایل بزرگ
- حداکثر اندازه: ۵۰MB
- پیشنهاد: PPTX را به چند قسمت تقسیم کنید

---

## 📈 ویژگی‌ها در آینده

- 🔄 پیش‌نمایش اسلایدها قبل از دانلود
- 🌍 پشتیبانی از زبان‌های عربی و ترکی
- 📱 برنامه موبایل (React Native)
- 🎯 تخصیص بیشتر حوزه‌های پزشکی
- 🔒 سیستم لاگین و مدیریت کاربران
- 📊 داشبورد آمار استفاده

---

## 🤝 مشارکت

Issues و Pull requests در GitHub خوش‌آمد هستند!

### راهنمای توسعه:
1. Fork پروژه
2. Branch جدید ایجاد کنید (`git checkout -b feature/AmazingFeature`)
3. تغییرات را commit کنید (`git commit -m 'Add some AmazingFeature'`)
4. Push کنید (`git push origin feature/AmazingFeature`)
5. Pull Request باز کنید

---

## 📜 لایسنس

این پروژه با لایسنس MIT منتشر شده است. استفاده شخصی و تجاری مجاز است.

---

## 📞 تماس

برای سوالات:
- Email: your-email@example.com
- GitHub Issues: [repository-issues]

---

## 🙏 تشکر ویژه

از Google Gemini team به خاطر API عالی Gemini 2.5
از FastAPI و Next.js communautés برای frameworkهای فوق‌العاده

🎉 موفق باشید در ترجمه متون پزشکی!

---

**نسخه:** 1.0.0
**تاریخ:** نوامبر ۲۰۲۵
**توسعه‌دهنده:** AI Assistant + Medical Experts
